## 一、知识点分区与掌握程度要求
### 🔵 基础概念区 — 要求：熟悉
| 知识点 | 掌握程度 | 核心要义 |
| --- | --- | --- |
| 网络编程概念 | **熟悉** | 计算机之间通过网络进行数据交换 |
| IP地址与端口 | **熟悉** | IP定位主机，端口定位进程 |
| TCP与UDP | **熟悉** | TCP面向连接可靠，UDP无连接不可靠 |
| HTTP协议 | **熟悉** | 请求-响应模型，方法、状态码、头信息 |
| RESTful API | **熟悉** | 用HTTP方法操作资源的设计风格 |


### 🟡 核心机制区 — 要求：理解（面试重点）
| 知识点 | 掌握程度 | 核心机制 |
| --- | --- | --- |
| Socket通信原理 | **理解** | 端到端通信的端点，TCP三次握手 |
| HTTP请求结构 | **理解** | 请求行、请求头、请求体 |
| HTTP响应结构 | **理解** | 状态行、响应头、响应体 |
| 常见状态码 | **理解** | 200成功、400客户端错误、500服务端错误 |


### 🟢 工程实践区 — 要求：精通（AI开发日常必备）
| 知识点 | 掌握程度 | 实践场景 |
| --- | --- | --- |
| requests库GET请求 | **精通** | 调用API获取数据 |
| requests库POST请求 | **精通** | 提交数据、调用模型API |
| 请求参数传递 | **精通** | params、data、json参数 |
| 请求头设置 | **精通** | 认证、内容类型设置 |
| 异常处理 | **精通** | 网络超时、连接错误处理 |
| 响应解析 | **精通** | JSON解析、状态码检查 |


### 🔴 扩展关联区 — 要求：了解
| 知识点 | 掌握程度 | 面试可能会问 |
| --- | --- | --- |
| Socket编程 | **了解** | TCP/UDP Socket基础 |
| WebSocket | **了解** | 全双工通信，实时应用 |
| FastAPI/Flask | **了解** | 模型服务部署框架 |
| 异步HTTP | **了解** | `aiohttp`异步请求 |


## 二、核心技能点（需精通）
1. **精通**：能用`requests.get()`发送GET请求
2. **精通**：能用`requests.post()`发送POST请求
3. **精通**：能设置请求头（headers）和超时（timeout）
4. **精通**：能处理响应状态码和JSON解析
5. **精通**：能处理网络请求异常



## 三、综合练习
### 入门关（15分钟）
**任务**：补全代码，让每个`print`输出正确结果。

```python
# 第一题：GET请求
import requests

response = requests.___("https://api.github.com")  # 发送GET
print(response.status_code)  # 预期输出: ___

# 第二题：POST请求
import json

url = "https://httpbin.org/post"
data = {"name": "张三", "age": 18}
response = requests.___(url, ___=data)  # POST请求，JSON数据
print(response.json()["json"]["name"])  # 预期输出: ___


# 第三题：请求头
headers = {
    "User-Agent": "Mozilla/5.0",
    "Authorization": "Bearer token123"
}
response = requests.get("https://httpbin.org/headers", headers=___)
print(response.json()["headers"]["User-Agent"])  # 预期输出: ___


# 第四题：异常处理
try:
    response = requests.get("https://不存在.com", timeout=1)
    response.raise_for_status()
except requests.exceptions.Timeout:
    print("超时")
except requests.exceptions.ConnectionError:
    print("连接失败")
# 预期输出: ___


# 第五题：查询参数
params = {"q": "Python", "page": 1}
response = requests.get("https://httpbin.org/get", ___=params)
print(response.json()["args"]["q"])  # 预期输出: ___
```

### 入门关参考答案
```python
# 第一题
response = requests.get("https://api.github.com")
print(response.status_code)  # 200

# 第二题
response = requests.post(url, json=data)
print(response.json()["json"]["name"])  # 张三

# 第三题
response = requests.get("https://httpbin.org/headers", headers=headers)
print(response.json()["headers"]["User-Agent"])  # Mozilla/5.0

# 第四题
# 预期输出: 连接失败

# 第五题
response = requests.get("https://httpbin.org/get", params=params)
print(response.json()["args"]["q"])  # Python
```



### 实战关（40分钟）
**任务**：实现一个LLM API调用工具，封装常见的API请求逻辑。

**背景**：AI应用需要调用OpenAI、HuggingFace等API，需要统一的调用接口。

**要求**：

1. 实现`LLMClient`类：
    - `__init__(self, base_url, api_key, timeout=30)`：初始化
    - `_request(self, endpoint, method="GET", data=None)`：底层请求方法
    - `chat(self, messages, model=None, **kwargs)`：对话请求
    - `embed(self, texts, model=None)`：向量化请求
    - `stream_chat(self, messages, **kwargs)`：流式对话（生成器）
2. 处理认证（API Key放在Header中）
3. 处理错误（状态码检查、异常捕获）
4. 支持自定义超时

**使用示例**：

```python
client = LLMClient(
    base_url="https://api.openai.com/v1",
    api_key="sk-xxx"
)

response = client.chat(
    messages=[{"role": "user", "content": "什么是AI？"}],
    model="gpt-3.5-turbo"
)
print(response)
```

### 实战关参考答案
```python
import requests
import json
import time

class LLMClient:
    """LLM API调用客户端"""
    
    def __init__(self, base_url, api_key, timeout=30):
        self.base_url = base_url.rstrip("/")
        self.api_key = api_key
        self.timeout = timeout
        self._headers = {
            "Content-Type": "application/json",
            "Authorization": f"Bearer {api_key}"
        }
        self._call_count = 0
    
    def _request(self, endpoint, method="GET", data=None, stream=False):
        """底层请求方法"""
        url = f"{self.base_url}/{endpoint.lstrip('/')}"
        self._call_count += 1
        
        try:
            if method.upper() == "GET":
                response = requests.get(
                    url,
                    headers=self._headers,
                    timeout=self.timeout,
                    stream=stream
                )
            elif method.upper() == "POST":
                response = requests.post(
                    url,
                    headers=self._headers,
                    json=data,
                    timeout=self.timeout,
                    stream=stream
                )
            else:
                raise ValueError(f"不支持的方法: {method}")
            
            response.raise_for_status()
            return response
            
        except requests.exceptions.Timeout:
            raise Exception(f"请求超时: {url}")
        except requests.exceptions.ConnectionError:
            raise Exception(f"连接失败: {url}")
        except requests.exceptions.HTTPError as e:
            status = response.status_code
            if status == 401:
                raise Exception("API密钥无效或已过期")
            elif status == 429:
                raise Exception("请求过于频繁，请稍后重试")
            elif status == 500:
                raise Exception("服务器内部错误")
            else:
                raise Exception(f"HTTP错误 {status}: {response.text}")
        except Exception as e:
            raise Exception(f"请求失败: {e}")
    
    def chat(self, messages, model="gpt-3.5-turbo", **kwargs):
        """对话请求"""
        data = {
            "model": model,
            "messages": messages,
            **kwargs
        }
        response = self._request("chat/completions", "POST", data)
        result = response.json()
        return result.get("choices", [{}])[0].get("message", {}).get("content")
    
    def embed(self, texts, model="text-embedding-ada-002"):
        """向量化请求"""
        if isinstance(texts, str):
            texts = [texts]
        data = {
            "model": model,
            "input": texts
        }
        response = self._request("embeddings", "POST", data)
        result = response.json()
        return [item.get("embedding") for item in result.get("data", [])]
    
    def stream_chat(self, messages, model="gpt-3.5-turbo", **kwargs):
        """流式对话（生成器）"""
        data = {
            "model": model,
            "messages": messages,
            "stream": True,
            **kwargs
        }
        response = self._request("chat/completions", "POST", data, stream=True)
        
        for line in response.iter_lines():
            if line:
                line = line.decode("utf-8")
                if line.startswith("data: "):
                    if line == "data: [DONE]":
                        break
                    try:
                        chunk = json.loads(line[6:])
                        content = chunk.get("choices", [{}])[0].get("delta", {}).get("content")
                        if content:
                            yield content
                    except json.JSONDecodeError:
                        continue
    
    def get_stats(self):
        """获取统计信息"""
        return {
            "call_count": self._call_count
        }


# ===== 测试（模拟） =====
def test_llm_client():
    # 使用httpbin模拟
    class MockLLMClient(LLMClient):
        def _request(self, endpoint, method="GET", data=None, stream=False):
            # 模拟响应
            if "chat" in endpoint:
                return self._mock_response({
                    "choices": [{"message": {"content": f"模拟回复: {data.get('messages', [])[-1].get('content')}"}}]
                })
            elif "embed" in endpoint:
                return self._mock_response({
                    "data": [{"embedding": [0.1, 0.2, 0.3]}] * len(data.get("input", []))
                })
            else:
                return super()._request(endpoint, method, data, stream)
        
        def _mock_response(self, data):
            class MockResponse:
                def __init__(self, data):
                    self.data = data
                def json(self):
                    return self.data
                def raise_for_status(self):
                    pass
                def iter_lines(self):
                    yield b"data: " + json.dumps({"choices": [{"delta": {"content": "模拟流式"}}]}).encode()
                    yield b"data: [DONE]"
            return MockResponse(data)
    
    client = MockLLMClient("https://api.openai.com/v1", "sk-mock")
    
    # 对话
    response = client.chat(
        messages=[{"role": "user", "content": "什么是AI？"}]
    )
    print(f"对话回复: {response}")
    
    # 向量化
    embeds = client.embed(["hello world", "AI is great"])
    print(f"向量维度: {len(embeds)} 个")
    print(f"第一个向量长度: {len(embeds[0])}")
    
    # 流式
    print("流式回复:")
    for chunk in client.stream_chat([{"role": "user", "content": "讲个笑话"}]):
        print(chunk, end="")
    print()
    
    print(f"统计: {client.get_stats()}")

if __name__ == "__main__":
    test_llm_client()
```



### 挑战关（60分钟）
**任务**：实现一个模型部署服务客户端，支持加载配置、请求模型、处理结果。

**背景**：部署在服务端的模型需要客户端调用，支持多种模型和批处理。

**要求**：

1. 实现`ModelClient`类：
    - `__init__(self, config_path)`：从配置文件加载服务地址和认证信息
    - `predict(self, inputs, model_name=None, **kwargs)`：同步预测
    - `predict_async(self, inputs, model_name=None, **kwargs)`：异步预测
    - `batch_predict(self, inputs_list, model_name=None, **kwargs)`：批量预测
    - `get_model_info(self, model_name)`：获取模型信息
2. 配置文件格式（JSON或YAML）
3. 支持负载均衡（多个服务地址）
4. 实现重试机制
5. 记录请求日志

**使用示例**：

```python
# 配置文件 config.json
{
    "service_urls": ["http://localhost:8000", "http://localhost:8001"],
    "api_key": "service-key",
    "timeout": 30,
    "retry_count": 3,
    "models": {
        "bert": {"endpoint": "/predict/bert"},
        "gpt": {"endpoint": "/predict/gpt"}
    }
}

client = ModelClient("config.json")
result = client.predict("Hello world", model_name="bert")
print(result)
```

### 挑战关参考答案
```python
import json
import time
import random
import requests
from concurrent.futures import ThreadPoolExecutor, as_completed

class ModelClient:
    """模型服务客户端"""
    
    def __init__(self, config_path):
        with open(config_path, "r", encoding="utf-8") as f:
            config = json.load(f)
        
        self.service_urls = config.get("service_urls", [])
        self.api_key = config.get("api_key", "")
        self.timeout = config.get("timeout", 30)
        self.retry_count = config.get("retry_count", 3)
        self.models = config.get("models", {})
        self._current_idx = 0
        self._call_log = []
    
    def _get_next_url(self):
        """负载均衡：轮询"""
        url = self.service_urls[self._current_idx % len(self.service_urls)]
        self._current_idx += 1
        return url
    
    def _request(self, endpoint, data, retry_count=None):
        """发送请求（带重试）"""
        if retry_count is None:
            retry_count = self.retry_count
        
        url = self._get_next_url() + endpoint
        headers = {
            "Content-Type": "application/json",
            "Authorization": f"Bearer {self.api_key}"
        }
        
        last_error = None
        for attempt in range(1, retry_count + 1):
            try:
                response = requests.post(
                    url,
                    headers=headers,
                    json=data,
                    timeout=self.timeout
                )
                response.raise_for_status()
                
                # 记录日志
                self._call_log.append({
                    "time": time.time(),
                    "url": url,
                    "status": "success",
                    "attempt": attempt
                })
                
                return response.json()
                
            except requests.exceptions.Timeout:
                last_error = "超时"
                delay = 0.5 * (2 ** (attempt - 1))
                time.sleep(delay)
            except requests.exceptions.ConnectionError:
                last_error = "连接失败"
                delay = 0.5 * (2 ** (attempt - 1))
                time.sleep(delay)
            except requests.exceptions.HTTPError as e:
                last_error = f"HTTP错误: {e}"
                if response.status_code in [401, 403]:
                    break  # 认证错误不重试
                delay = 0.5 * (2 ** (attempt - 1))
                time.sleep(delay)
            except Exception as e:
                last_error = str(e)
                break
        
        self._call_log.append({
            "time": time.time(),
            "url": url,
            "status": "failed",
            "error": last_error
        })
        
        raise Exception(f"请求失败 (重试{retry_count}次): {last_error}")
    
    def predict(self, inputs, model_name=None, **kwargs):
        """同步预测"""
        if model_name is None:
            model_name = list(self.models.keys())[0]
        
        model_config = self.models.get(model_name)
        if model_config is None:
            raise ValueError(f"模型 '{model_name}' 未配置")
        
        endpoint = model_config.get("endpoint", f"/predict/{model_name}")
        data = {"inputs": inputs, **kwargs}
        
        result = self._request(endpoint, data)
        return result.get("result", result)
    
    def batch_predict(self, inputs_list, model_name=None, max_workers=5, **kwargs):
        """批量预测（多线程）"""
        results = [None] * len(inputs_list)
        
        def process(idx, inputs):
            try:
                result = self.predict(inputs, model_name, **kwargs)
                return idx, result, None
            except Exception as e:
                return idx, None, str(e)
        
        with ThreadPoolExecutor(max_workers=max_workers) as executor:
            futures = [
                executor.submit(process, idx, inputs)
                for idx, inputs in enumerate(inputs_list)
            ]
            
            for future in as_completed(futures):
                idx, result, error = future.result()
                if error:
                    results[idx] = f"<错误: {error}>"
                else:
                    results[idx] = result
        
        return results
    
    def get_model_info(self, model_name):
        """获取模型信息"""
        model_config = self.models.get(model_name)
        if model_config is None:
            return None
        return {
            "name": model_name,
            "endpoint": model_config.get("endpoint"),
            "config": model_config.get("config", {})
        }
    
    def get_stats(self):
        """获取统计信息"""
        total = len(self._call_log)
        success = sum(1 for log in self._call_log if log["status"] == "success")
        return {
            "total_requests": total,
            "success_requests": success,
            "failure_requests": total - success,
            "success_rate": round(success / total * 100, 1) if total > 0 else 0
        }


# ===== 测试 =====
def test_model_client():
    # 创建模拟配置
    config = {
        "service_urls": ["http://localhost:8000", "http://localhost:8001"],
        "api_key": "test-key",
        "timeout": 5,
        "retry_count": 2,
        "models": {
            "bert": {"endpoint": "/predict/bert", "config": {"max_length": 512}},
            "gpt": {"endpoint": "/predict/gpt", "config": {"max_tokens": 100}}
        }
    }
    
    with open("test_config.json", "w") as f:
        json.dump(config, f)
    
    # 模拟请求的客户端
    class MockModelClient(ModelClient):
        def _request(self, endpoint, data, retry_count=None):
            # 模拟：90%成功
            if random.random() < 0.1:
                raise Exception("模拟服务故障")
            return {"result": f"处理: {data.get('inputs')}"}
    
    client = MockModelClient("test_config.json")
    
    print("=== 单次预测 ===")
    result = client.predict("Hello world", model_name="bert")
    print(f"结果: {result}")
    
    print("\n=== 批量预测 ===")
    inputs = [f"text_{i}" for i in range(10)]
    results = client.batch_predict(inputs, model_name="gpt", max_workers=3)
    for i, r in enumerate(results[:5]):
        print(f"  {inputs[i]} -> {r}")
    
    print(f"\n=== 统计信息 ===")
    print(client.get_stats())
    
    print("\n=== 模型信息 ===")
    print(client.get_model_info("bert"))


if __name__ == "__main__":
    test_model_client()
```



## 四、面试高频题
| 问题 | 参考答案 |
| --- | --- |
| `requests.get()`和`requests.post()`的区别？ | GET用于获取数据，参数在URL中；POST用于提交数据，数据在请求体中 |
| 什么是HTTP状态码200、400、500？ | 200成功，400客户端错误，500服务器错误 |
| 如何设置请求超时？ | `requests.get(url, timeout=5)` |
| 如何处理响应JSON？ | `response.json()` |
| 什么是RESTful API？ | 用HTTP方法（GET/POST/PUT/DELETE）操作资源的设计风格 |
| 调用API时如何处理认证？ | 在Header中添加`Authorization: Bearer {token}` |
| Socket和HTTP的关系？ | Socket是底层通信，HTTP是应用层协议，基于TCP Socket |


## 五、学习验证清单
| 问题 | 自评 |
| --- | --- |
| 我能用`requests.get()`发送GET请求 | ☐ |
| 我能用`requests.post()`发送POST请求 | ☐ |
| 我能设置请求头和认证信息 | ☐ |
| 我能传递查询参数和请求体数据 | ☐ |
| 我能处理响应状态码和JSON解析 | ☐ |
| 我能处理网络请求异常 | ☐ |
| 我知道常用的HTTP状态码含义 | ☐ |
| 我知道RESTful API的基本概念 | ☐ |
| 我能调用LLM API获取响应 | ☐ |
| 我知道如何设置请求超时 | ☐ |


