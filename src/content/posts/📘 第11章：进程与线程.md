---
title: 📘 第11章：进程与线程
published: 2026-06-30
description: 📘 第11章：进程与线程
---

## 一、知识点分区与掌握程度要求
### 🔵 基础概念区 — 要求：熟悉
| 知识点 | 掌握程度 | 核心要义 |
| --- | --- | --- |
| 并发与并行 | **熟悉** | 并发是交替执行，并行是同时执行 |
| 进程概念 | **熟悉** | 资源分配的基本单位，有独立内存空间 |
| 线程概念 | **熟悉** | 任务调度的基本单位，共享进程内存 |
| 进程与线程区别 | **熟悉** | 进程独立、开销大；线程共享、开销小 |


### 🟡 核心机制区 — 要求：理解（面试重点）
| 知识点 | 掌握程度 | 核心机制 |
| --- | --- | --- |
| GIL全局解释器锁 | **理解** | CPython中同一时刻只允许一个线程执行Python字节码，面试必考 |
| 互斥锁原理 | **理解** | 保证共享数据操作的原子性，防止数据竞争 |
| 进程间通信 | **理解** | Queue实现进程间数据传递 |
| 线程安全问题 | **理解** | 多线程修改共享变量需要加锁 |


### 🟢 工程实践区 — 要求：精通（AI开发实际使用场景）
| 知识点 | 掌握程度 | 实践场景 |
| --- | --- | --- |
| multiprocessing.Process | **精通** | 数据预处理并行加速 |
| 进程池Pool | **精通** | 批量数据处理任务分发 |
| threading.Thread | **精通** | API并发请求、IO密集型任务 |
| 线程池ThreadPoolExecutor | **精通** | 管理多个并发任务 |
| 互斥锁Lock | **精通** | 保护共享资源 |


### 🔴 扩展关联区 — 要求：了解
| 知识点 | 掌握程度 | 面试可能会问 |
| --- | --- | --- |
| 死锁 | **了解** | 知道死锁产生条件 |
| 异步IO | **了解** | `asyncio`与多线程的区别 |
| 进程vs线程性能 | **了解** | CPU密集用进程，IO密集用线程 |


## 二、核心技能点（需精通）
1. **精通**：能用multiprocessing.Process创建多进程
2. **精通**：能用进程池Pool并行处理任务
3. **精通**：能用threading.Thread创建多线程
4. **精通**：能用线程池ThreadPoolExecutor管理并发
5. **精通**：能用Lock实现互斥锁保护共享资源



## 三、综合练习
### 入门关（20分钟）
**任务**：补全代码，让每个`print`输出正确结果。

```python
# 第一题：创建进程
import multiprocessing
import os

def print_pid():
    print(f"进程ID: {os.getpid()}")

if __name__ == "__main__":
    p = multiprocessing.Process(target=print_pid)
    p.___()  # 启动进程
    p.___()  # 等待进程结束


# 第二题：进程池
def square(n):
    return n * n

if __name__ == "__main__":
    with multiprocessing.Pool(processes=3) as pool:
        results = pool.___  # 使用map并行计算
        print(results)  # 预期输出: [1, 4, 9, 16, 25]


# 第三题：创建线程
import threading
import time

def print_hello():
    time.sleep(0.1)
    print("Hello")

if __name__ == "__main__":
    t = threading.Thread(target=print_hello)
    t.___()  # 启动线程


# 第四题：线程池
from concurrent.futures import ThreadPoolExecutor

def add(a, b):
    return a + b

with ThreadPoolExecutor(max_workers=2) as executor:
    future = executor.___(add, 3, 5)  # 提交任务
    result = future.___()  # 获取结果
    print(result)  # 预期输出: 8


# 第五题：互斥锁
import threading
counter = 0
lock = threading.Lock()

def increment():
    global counter
    for _ in range(1000):
        with lock:  # 使用with自动获取和释放锁
            counter += 1

threads = [threading.Thread(target=increment) for _ in range(5)]
for t in threads:
    t.start()
for t in threads:
    t.join()
print(counter)  # 预期输出: ___
```

### 入门关参考答案
```python
# 第一题
p.start()
p.join()

# 第二题
results = pool.map(square, [1, 2, 3, 4, 5])

# 第三题
t.start()

# 第四题
future = executor.submit(add, 3, 5)
result = future.result()

# 第五题
print(counter)  # 5000
```



### 实战关（40分钟）
**任务**：实现一个批量数据预处理工具，使用多进程加速处理。

**背景**：AI训练中需要对大量文本数据进行预处理（分词、清洗、向量化），单进程处理慢，需要并行加速。

**要求**：

1. 实现`DataPreprocessor`类：
    - `__init__(self, num_workers=4)`：指定进程数
    - `process_batch(self, texts, processor)`：多进程处理文本列表
    - `process_file(self, filepath, processor)`：处理文件中的文本
2. 使用进程池Pool并行处理
3. 使用Queue收集处理结果
4. 统计处理时间

**使用示例**：

```python
# 定义处理函数
def clean_text(text):
    import re
    return re.sub(r'[^a-zA-Z\s]', '', text).lower()

# 批量处理
texts = ["Hello, World!", "Python 3.9", "AI is great!"]
preprocessor = DataPreprocessor(num_workers=3)
results = preprocessor.process_batch(texts, clean_text)
print(results)  # ['hello world', 'python', 'ai is great']
```

### 实战关参考答案
```python
import multiprocessing
import time
import re
from functools import partial

class DataPreprocessor:
    """数据预处理器（多进程）"""
    
    def __init__(self, num_workers=None):
        if num_workers is None:
            num_workers = multiprocessing.cpu_count()
        self.num_workers = num_workers
        self._processed_count = 0
    
    def _worker(self, text, processor):
        """工作进程执行的处理函数"""
        result = processor(text)
        self._processed_count += 1
        return result
    
    def process_batch(self, texts, processor, batch_size=100):
        """批量处理文本（多进程）"""
        if not texts:
            return []
        
        start_time = time.time()
        
        # 分批处理
        batches = [texts[i:i+batch_size] for i in range(0, len(texts), batch_size)]
        
        results = []
        with multiprocessing.Pool(processes=self.num_workers) as pool:
            # 使用map并行处理
            for batch in batches:
                batch_results = pool.map(processor, batch)
                results.extend(batch_results)
        
        elapsed = time.time() - start_time
        print(f"处理完成: {len(texts)} 条文本, 耗时 {elapsed:.2f}秒")
        
        return results
    
    def process_batch_async(self, texts, processor, batch_size=100):
        """异步批量处理（返回AsyncResult）"""
        batches = [texts[i:i+batch_size] for i in range(0, len(texts), batch_size)]
        
        with multiprocessing.Pool(processes=self.num_workers) as pool:
            # 使用map_async异步执行
            async_result = pool.map_async(processor, texts)
            # 可以继续做其他事情...
            results = async_result.get(timeout=60)
        
        return results

# ===== 测试 =====
if __name__ == "__main__":
    def clean_text(text):
        """清洗文本"""
        # 去除非字母字符，转小写
        cleaned = re.sub(r'[^a-zA-Z\s]', '', text)
        return cleaned.lower()
    
    def tokenize(text):
        """简单分词"""
        return text.split()
    
    # 准备测试数据
    texts = [
        "Hello, World! This is a test.",
        "Python 3.9 is great for AI development.",
        "Machine Learning: supervised and unsupervised.",
        "Deep Learning uses neural networks.",
        "Natural Language Processing is fun!"
    ] * 20  # 重复100条
    
    preprocessor = DataPreprocessor(num_workers=4)
    
    print("=== 清洗处理 ===")
    cleaned = preprocessor.process_batch(texts, clean_text)
    print(f"清洗示例: {cleaned[:2]}")
    
    print("\n=== 分词处理 ===")
    tokenized = preprocessor.process_batch(texts, tokenize)
    print(f"分词示例: {tokenized[:2]}")
```



### 挑战关（60分钟）
**任务**：实现一个并发API调用器，使用多线程和线程池并发请求。

**背景**：AI应用需要批量调用API（如OpenAI、HuggingFace），IO密集型任务适合用多线程加速。

**要求**：

1. 实现`ConcurrentAPICaller`类：
    - `__init__(self, max_workers=10, retry_count=3)`：配置线程池和重试
    - `call(self, func, *args, **kwargs)`：单次调用
    - `call_batch(self, func, items, concurrency=10)`：并发批量调用
    - `call_stream(self, func, items)`：流式调用（生成器）
2. 实现重试机制（指数退避）
3. 使用线程池`ThreadPoolExecutor`
4. 使用互斥锁保护共享计数器
5. 处理异常和超时

**使用示例**：

```python
def mock_api(text):
    """模拟API调用"""
    import random, time
    time.sleep(random.uniform(0.1, 0.5))
    if random.random() < 0.05:
        raise Exception("API临时故障")
    return f"处理: {text}"

caller = ConcurrentAPICaller(max_workers=5)
results = caller.call_batch(mock_api, ["text1", "text2", "text3", "text4", "text5"])
print(results)
```

### 挑战关参考答案
```python
import time
import random
import threading
from concurrent.futures import ThreadPoolExecutor, as_completed
from collections import deque

class ConcurrentAPICaller:
    """并发API调用器"""
    
    def __init__(self, max_workers=10, retry_count=3, timeout=30):
        self.max_workers = max_workers
        self.retry_count = retry_count
        self.timeout = timeout
        self._success_count = 0
        self._failure_count = 0
        self._lock = threading.Lock()
        self._results = []
    
    def _call_with_retry(self, func, *args, **kwargs):
        """带重试的调用"""
        last_error = None
        for attempt in range(1, self.retry_count + 1):
            try:
                result = func(*args, **kwargs)
                # 成功
                with self._lock:
                    self._success_count += 1
                return result
            except Exception as e:
                last_error = e
                if attempt < self.retry_count:
                    # 指数退避
                    delay = 0.5 * (2 ** (attempt - 1))
                    print(f"重试 {attempt}/{self.retry_count}，等待 {delay:.1f}秒")
                    time.sleep(delay)
        
        # 所有重试失败
        with self._lock:
            self._failure_count += 1
        raise last_error
    
    def call(self, func, *args, **kwargs):
        """单次调用"""
        return self._call_with_retry(func, *args, **kwargs)
    
    def call_batch(self, func, items, concurrency=None, **kwargs):
        """并发批量调用"""
        if concurrency is None:
            concurrency = self.max_workers
        
        results = [None] * len(items)
        self._success_count = 0
        self._failure_count = 0
        
        start_time = time.time()
        
        with ThreadPoolExecutor(max_workers=concurrency) as executor:
            # 提交所有任务
            future_to_idx = {}
            for idx, item in enumerate(items):
                future = executor.submit(self._call_with_retry, func, item, **kwargs)
                future_to_idx[future] = idx
            
            # 收集结果
            for future in as_completed(future_to_idx):
                idx = future_to_idx[future]
                try:
                    results[idx] = future.result(timeout=self.timeout)
                except Exception as e:
                    results[idx] = f"<错误: {str(e)}>"
        
        elapsed = time.time() - start_time
        print(f"批量完成: {len(items)} 项, 成功: {self._success_count}, "
              f"失败: {self._failure_count}, 耗时: {elapsed:.2f}秒")
        
        return results
    
    def call_batch_with_progress(self, func, items, concurrency=None, **kwargs):
        """带进度显示的批量调用"""
        if concurrency is None:
            concurrency = self.max_workers
        
        results = [None] * len(items)
        completed = 0
        total = len(items)
        lock = threading.Lock()
        
        def process_item(idx, item):
            nonlocal completed
            try:
                result = self._call_with_retry(func, item, **kwargs)
                results[idx] = result
            except Exception as e:
                results[idx] = f"<错误: {str(e)}>"
            finally:
                with lock:
                    completed += 1
                    if completed % max(1, total // 20) == 0 or completed == total:
                        print(f"进度: {completed}/{total} ({completed/total*100:.0f}%)")
        
        start_time = time.time()
        
        with ThreadPoolExecutor(max_workers=concurrency) as executor:
            futures = []
            for idx, item in enumerate(items):
                future = executor.submit(process_item, idx, item)
                futures.append(future)
            
            # 等待所有任务完成
            for future in futures:
                future.result(timeout=self.timeout * 2)
        
        elapsed = time.time() - start_time
        print(f"批量完成: {total} 项, 耗时: {elapsed:.2f}秒")
        
        return results
    
    def get_stats(self):
        """获取统计信息"""
        return {
            "success_count": self._success_count,
            "failure_count": self._failure_count,
            "total": self._success_count + self._failure_count,
            "success_rate": round(
                self._success_count / (self._success_count + self._failure_count) * 100,
                1
            ) if (self._success_count + self._failure_count) > 0 else 0
        }

# ===== 测试 =====
if __name__ == "__main__":
    def mock_api(text):
        """模拟API调用"""
        time.sleep(random.uniform(0.1, 0.3))
        if random.random() < 0.03:
            raise Exception("API临时故障")
        return f"处理: {text.upper()}"
    
    caller = ConcurrentAPICaller(max_workers=5, retry_count=2)
    
    print("=== 并发批量调用 ===")
    items = [f"text_{i}" for i in range(20)]
    results = caller.call_batch(mock_api, items, concurrency=5)
    
    for i, result in enumerate(results[:5]):
        print(f"{items[i]} -> {result}")
    
    print(f"\n统计: {caller.get_stats()}")
    
    print("\n=== 带进度批量调用 ===")
    items2 = [f"item_{i}" for i in range(50)]
    results2 = caller.call_batch_with_progress(mock_api, items2, concurrency=10)
    print(f"完成: {len(results2)} 项")
```



## 四、面试高频题
| 问题 | 参考答案 |
| --- | --- |
| 什么是GIL？对Python多线程有什么影响？ | CPython的全局解释器锁，同一时刻只允许一个线程执行Python字节码。CPU密集型任务多线程无效，IO密集型仍有收益 |
| 进程和线程的区别？ | 进程有独立内存空间，开销大；线程共享内存空间，开销小。进程崩溃不影响其他进程，线程崩溃影响整个进程 |
| 什么时候用多进程，什么时候用多线程？ | CPU密集型用多进程（绕过GIL），IO密集型用多线程（GIL在IO时释放） |
| 线程安全问题如何解决？ | 使用互斥锁`Lock`，确保共享资源操作的原子性 |
| `multiprocessing.Queue`和`Manager().Queue`的区别？ | `multiprocessing.Queue`用于普通进程通信，`Manager().Queue`用于进程池中的进程通信 |
| 进程池和线程池的区别？ | 进程池管理进程，线程池管理线程；用途类似，底层资源不同 |


## 五、学习验证清单
| 问题 | 自评 |
| --- | --- |
| 我能用multiprocessing.Process创建多进程 | ☐ |
| 我能用进程池Pool并行处理任务 | ☐ |
| 我能用threading.Thread创建多线程 | ☐ |
| 我能用线程池ThreadPoolExecutor管理并发 | ☐ |
| 我能用Lock保护共享资源 | ☐ |
| 我知道GIL是什么及其影响 | ☐ |
| 我知道进程和线程的区别 | ☐ |
| 我知道什么时候用进程、什么时候用线程 | ☐ |
| 我知道多线程在IO密集型任务中的优势 | ☐ |
| 我知道为什么CPU密集型任务要用多进程 | ☐ |


